# =============================================================================
# Python Automation Dependencies
# SQL Server → PostgreSQL Migration Project
# =============================================================================
#
# Purpose: Dependencies for automation scripts that reduce manual work
# Scripts: analyze-procedure.py, compare-versions.py, extract-warnings.py, etc.
#
# Installation:
#   python -m venv .venv
#   source .venv/bin/activate  # Linux/Mac
#   pip install -r scripts/automation/requirements.txt
#
# =============================================================================

# SQL Parsing & Analysis
sqlparse>=0.4.3         # SQL parsing and formatting
regex>=2023.10.3        # Advanced regular expressions for pattern matching

# Template Engine & Configuration
jinja2>=3.1.2          # Template engine for generating documents
pyyaml>=6.0.1          # YAML configuration file support

# Data Processing & Reporting
pandas>=2.1.0          # Data analysis and CSV/Excel reporting
beautifulsoup4>=4.12   # HTML parsing for compare-versions.py
lxml>=4.9.3            # XML parsing for AWS SCT report extraction

# CLI & User Interface
click>=8.1.7           # Command-line interface framework
rich>=13.6.0           # Rich terminal formatting and progress bars

# Optional but Recommended
colorama>=0.4.6        # Cross-platform colored terminal output (Windows)
tabulate>=0.9.0        # Pretty-print tabular data
python-dateutil>=2.8.2 # Date/time parsing utilities

# Development & Testing (Optional)
pytest>=7.4.0          # Testing framework
black>=23.9.0          # Code formatter
flake8>=6.1.0          # Linting
mypy>=1.5.0            # Type checking
